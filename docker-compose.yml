version: "3.8"

services:
  leadflow-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leadflow-scraper
    ports:
      - "4002:3002"
    volumes:
      - ./:/app   # for live reload in dev
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
